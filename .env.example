# Copy this file to `.env` and fill real values locally.
# Never commit `.env` with real secrets.

# GitHub token (fine-grained PAT or classic PAT)
# Used for GitHub API automation (e.g., create PR when `gh` is not available).
GITHUB_TOKEN=

